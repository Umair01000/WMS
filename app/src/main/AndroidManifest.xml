<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
        <uses-permission android:name="android.permission_ACCESS_NETWORK_STATE">

    </uses-permission>

    <application
        android:name=".application.WMSApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WMS"
        tools:targetApi="30">
        <activity
            android:name=".ui.user.UserHomeActivity"
            android:exported="false"
            android:label="@string/title_activity_employee"
            android:theme="@style/Theme.WMS.NoActionBar" />
        <activity
            android:name=".login"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.WMS.NoActionBar" />
        <activity
            android:name=".UserChoiceActivity"
            android:exported="false" />
        <activity android:name=".waterbill"
            android:exported="false"
            />
        <activity android:name=".complaints" />

        <activity
            android:name=".ApplyMeterActivity"
            />
        <activity
            android:name=".officelocations"
            />
        <activity
            android:name=".jobvacancies"
            android:exported="false"

            />

        <activity
            android:name=".helpline"
            />
        <activity android:name=".workschedule"
            android:exported="false"
            >

        </activity>

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".splash"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>